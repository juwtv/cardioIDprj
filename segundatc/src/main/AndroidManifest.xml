<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <!-- aidl -->
    <permission
        android:name="com.example.segundatc.permission.BIND_MY_SERVICE"
        android:label="Bind My Service"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.segundatc.permission.BIND_MY_SERVICE" />

    <!-- Android Auto -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:appCategory="audio"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:enableOnBackInvokedCallback="true">

        <!-- bound service -->
        <service
            android:name=".fragments.boundService.BoundService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.segundatc.BoundService" />
            </intent-filter>
        </service>

        <!-- android auto -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />
        <service
            android:name=".androidAuto.AndroidAutoService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.MEDIA" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- content provider -->
        <provider
            android:name=".fragments.contentProvider.MyContentProvider"
            android:authorities="com.example.segundatc.fragments.contentProvider"
            android:enabled="true"
            android:exported="true"
            android:grantUriPermissions="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </provider>

        <!-- socket -->
        <service android:name=".fragments.socket.MessageReceiverService" />

        <!-- broadcast receiver -->
        <receiver
            android:name=".fragments.broadcastReceiver.MyBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.ACTION_SEND_MESSAGE_BD" />
            </intent-filter>
        </receiver>

        <!-- intents -->
        <service
            android:name=".fragments.intent.IntentService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.segundatc.RECEIVE_ACTION" />
            </intent-filter>
        </service>

        <!-- aidl -->
        <service
            android:name=".fragments.aidl.AIDLService"
            android:exported="true"
            android:permission="com.example.segundatc.permission.BIND_MY_SERVICE">
            <intent-filter>
                <action android:name="com.example.segundatc.fragments.aidl.AIDLService" />
            </intent-filter>
        </service>


        <!-- inter-app comm -->
        <receiver android:name=".androidAuto.broadcast.AutoBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.segundatc.RECEIVE_ACTION_AUTO" />
            </intent-filter>
        </receiver>

    </application>

</manifest>